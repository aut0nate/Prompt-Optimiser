services:
  prompt-optimiser:
    build: .
    container_name: prompt-optimiser
    restart: unless-stopped
    ports:
      - "3001:3000"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 3000
